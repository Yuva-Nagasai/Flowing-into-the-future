{
  "name": "workspace",
  "version": "1.0.0",
  "description": "This repository now uses an explicit two-folder layout so the Vite React frontend and the Node/Express backend can evolve independently without getting in one another's way.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^24.10.1"
  }
}
